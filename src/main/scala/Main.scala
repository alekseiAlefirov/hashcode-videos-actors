import io.Parser._
import io.Printer
//import logic.Solver


object Main {

  /*def main(args: Array[String]): Unit = {
    val situation = parseFile(args(0))
    val solver = new Solver(situation)
    while (true) {
      if (solver.iterate()) {
        Printer.printSolution(Solver.scoredSolution2Solution(solver.bestSolution))
      }
    }
  }*/

}
